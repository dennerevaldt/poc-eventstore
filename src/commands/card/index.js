const { addCard } = require('./AddCard');
const { removeCard } = require('./RemoveCard');
const { getValidCards } = require('./GetCard');

module.exports = {
  addCard,
  removeCard,
  getValidCards,
};
